// Bitcoin1070 PRO v17.3.1 - premium home market pulse
(()=>{'use strict';const ok=v=>v!==null&&v!==undefined&&v!==''&&Number.isFinite(Number(v)),num=(v,d=2)=>ok(v)?Number(v).toLocaleString('ja-JP',{maximumFractionDigits:d}):'未取得',yen=v=>ok(v)?`¥${Math.round(Number(v)).toLocaleString('ja-JP')}`:'未取得',usd=v=>ok(v)?`$${Number(v).toLocaleString('en-US',{maximumFractionDigits:2})}`:'未取得',esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));function value(r){if(r.key==='BTC'){const j=ok(r.valueJpy)?r.valueJpy:r.value;return `<span>${yen(j)}</span><small class="btc-usd-price">${usd(r.valueUsd)}</small>`}if(!ok(r.value))return'未取得';if(r.key==='USDJPY')return `¥${num(r.value,3)}`;if(r.key==='US10Y')return `${num(r.value,3)}%`;return num(r.value,2)}function cls(r){const out=['market-tile'];if(r.key==='BTC')out.push('market-btc');if(r.key==='FEAR')out.push('market-fear');if(ok(r.changePct))out.push(Number(r.changePct)>=0?'market-up':'market-down');return out.join(' ')}function source(r){if(r.key==='BTC'&&r.source==='Bitcoin1070 保存値')return'保存値';if(r.key==='BTC'&&r.source==='Bitcoin1070 Home Market')return'保存値';if(r.source==='Bitcoin1070 API'||r.source==='CoinGecko'||r.source==='Yahoo Finance')return'LIVE';return r.source||'未取得'}function tile(r){const ch=ok(r.changePct)?`${Number(r.changePct)>0?'+':''}${Number(r.changePct).toFixed(2)}%`:'--';return `<div class="${cls(r)}"><span>${esc(r.label)}</span><strong>${value(r)}</strong><small>${ch} ・ ${esc(source(r))}</small></div>`}let timer=null;async function load(){const root=document.getElementById('homeMarketIndicators'),q=document.getElementById('homeMarketQuality');if(!root||!window.Bitcoin1070InvestmentTools)return;try{const d=await Bitcoin1070InvestmentTools.marketIndicators();const rows=[...d.rows].sort((a,b)=>{const order={BTC:0,USDJPY:1,NIKKEI:2,NASDAQ:3,SP500:4,US10Y:5,VIX:6,FEAR:7};return(order[a.key]??99)-(order[b.key]??99)});root.innerHTML=rows.map(tile).join('');q.textContent=`LIVE DATA ${d.quality}/100 ・ ${new Date(d.observedAt).toLocaleTimeString('ja-JP',{hour:'2-digit',minute:'2-digit'})}`}catch(_){root.innerHTML='<div class="market-tile"><span>市場指標</span><strong>取得失敗</strong><small>再読み込みしてください</small></div>';q.textContent='LIVE DATA 0/100'}}function start(){load();if(timer)clearInterval(timer);timer=setInterval(()=>{if(document.visibilityState==='visible')load()},30000)}document.addEventListener('visibilitychange',()=>{if(document.visibilityState==='visible')load()});if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',start,{once:true});else start()})();