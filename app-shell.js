// Bitcoin1070 PRO v16.0 - common app shell
(()=>{const page=location.pathname.split('/').pop()||'index.html',pageGroup=page==='portfolio-edit.html'||page==='asset-intelligence.html'?'portfolio.html':page==='analysis.html'?'market.html':page,RELEASE='16.0',RELOAD_KEY=`bitcoin1070_release_reload_${RELEASE}`;function setActiveNav(){document.querySelectorAll('.bottom-nav a').forEach(link=>{const href=(link.getAttribute('href')||'').split('#')[0],active=href===pageGroup;link.classList.toggle('active',active);if(active)link.setAttribute('aria-current','page');else link.removeAttribute('aria-current')})}function addBackButton(){if(!['portfolio-edit.html','analysis.html','asset-intelligence.html'].includes(page))return;const header=document.querySelector('.app-header');if(!header||header.querySelector('.smart-back-button'))return;const button=document.createElement('button');button.type='button';button.className='smart-back-button';button.textContent='← 戻る';button.addEventListener('click',()=>{if(history.length>1)history.back();else location.href=page==='analysis.html'?'market.html':'portfolio.html'});header.prepend(button)}function restoreScroll(){const key=`btc1070_scroll_${page}`,saved=Number(sessionStorage.getItem(key)||0);if(saved>0)requestAnimationFrame(()=>window.scrollTo({top:saved,behavior:'auto'}));let timer;window.addEventListener('scroll',()=>{clearTimeout(timer);timer=setTimeout(()=>sessionStorage.setItem(key,String(window.scrollY)),120)},{passive:true})}function setupNetworkStatus(){const banner=document.createElement('div');banner.className='network-banner hidden';banner.setAttribute('role','status');banner.textContent='オフラインです。保存済みデータを表示しています。';document.body.appendChild(banner);const refresh=()=>banner.classList.toggle('hidden',navigator.onLine);addEventListener('online',refresh);addEventListener('offline',refresh);refresh()}function reloadForRelease(){if(sessionStorage.getItem(RELOAD_KEY)==='1')return;sessionStorage.setItem(RELOAD_KEY,'1');const url=new URL(location.href);url.searchParams.set('_release',RELEASE);location.replace(url.toString())}function registerServiceWorker(){if(!('serviceWorker'in navigator))return;let refreshing=false;navigator.serviceWorker.addEventListener('controllerchange',()=>{if(refreshing)return;refreshing=true;reloadForRelease()});addEventListener('load',async()=>{try{const registration=await navigator.serviceWorker.register(`./service-worker.js?v=${RELEASE}`,{updateViaCache:'none'});if(registration.waiting)registration.waiting.postMessage({type:'SKIP_WAITING'});registration.addEventListener('updatefound',()=>{const worker=registration.installing;if(!worker)return;worker.addEventListener('statechange',()=>{if(worker.state==='installed'&&navigator.serviceWorker.controller)worker.postMessage({type:'SKIP_WAITING'})})});await registration.update()}catch(error){console.warn('Service Worker registration failed:',error)}})}document.addEventListener('DOMContentLoaded',()=>{setActiveNav();addBackButton();restoreScroll();setupNetworkStatus()});registerServiceWorker()})();
